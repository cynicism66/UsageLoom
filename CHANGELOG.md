# 变更日志

## Windows 0.8.34 — 2026-09-23

- 统一 Claude 与 Codex 概览日期筛选按钮的水平间距，修复两排按钮看起来疏密不一致的问题；加入界面回归检查。

## Windows 0.8.33 — 2026-09-23

- Claude 概览和当前额度卡片新增实验性的每周容量参考：只把同一来源、同一周周期的连续额度增长与同期本机 Claude Code Token 配对，再展示同工作负载的 Token／周；可定价覆盖充分时附 API 等价值。
- 缺少新鲜额度、重置时间、本机完整日志或足够配对时显示具体原因；发现未匹配的额度增长或样本比例不稳定时不外推。官方剩余百分比保持独立，不被估算覆盖。
- 托盘 Claude 卡片的悬停说明显示参考状态；新增回归测试覆盖来源、周期、缺口和价格覆盖边界。

## Windows 0.8.32 — 2026-09-23

- 修正 Claude 统计的“自定义”日期筛选：与 Codex 共用“开始／结束”标签、日期格式和响应式排列；移除 Claude 独有的重复日期范围摘要。
- 增加界面回归检查，确保两种 AI 的日期控件保持一致。

## Windows 0.8.31 — 2026-09-23

- 设置页的更新节奏、通知、容量估算与登录相关区域改为每类包含 Codex／Claude 独立卡片；不支持的 Claude 选项显示边界说明，软件更新等全局设置保持独立。
- Claude 概览与 Codex 统一为五种日期范围、三栏本机历史分解及四栏用量指标；新增所选本机 Claude Code Token 的 API 等价部分估算和定价覆盖率。
- Claude 估价按官方公开模型价格区分普通输入、输出、缓存读取及 5 分钟／1 小时写入；未知模型不按零收费。估算不代表 Pro 账单或全账号每周容量。

## Windows 0.8.30 — 2026-09-23

- 概览、模型与项目、Session 明细的 Codex／Claude 切换按钮移到页面固定标题区，滚动内容时保持可见；移除重复的“使用统计”标题和说明。
- Claude 概览改用与 Codex 一致的汇总指标、Token 趋势、模型分布、最近 Session 和 Token 构成布局，并复用趋势与模型图表样式。
- 只有可识别的本机 Claude Code 日志才填写 Token 等指标；未取得时显示“—”与来源说明，不把 Claude 套餐额度百分比换算为 Token，也不将缺失数据当作零。

## Windows 0.8.29 — 2026-09-22

- 新增本机 Claude Code JSONL Token 读取，默认关闭，可设置独立 Home；提供今日小时趋势、近 7／30 天日趋势、模型、项目和匿名 Session 汇总。
- 按回复与请求身份去重，处理流式记录、跨文件副本和子代理父回复重放；分列普通输入、输出、缓存读取与缓存写入，不重复累计缓存细分字段。
- 明确缺失日志、无可识别记录、部分读取与失败状态；仅在内存缓存白名单元数据，不保存正文、日志副本或凭据，不上传数据。
- Claude Token 统计成为主区域，额度快照历史改为辅助折叠区；可连接的同周期快照改用阶梯线，详细数据质量说明收进提示。
- Token 仅代表所选本机日志，不等于 Claude 全部用量、当前账号、套餐额度或权威账单。

## Windows 0.8.28 — 2026-09-22

- Claude 概览新增额度历史，导入可读取的本地历史并保存后续快照，支持近 1／7／30 天筛选、5 小时与每周百分比趋势、周期记录及最近观测明细。
- 历史按缓存来源和额度窗口隔离，重复读取不重复计数；重置、回退、冲突和长间隔不连线计算，缺少重置时间时只展示观测点。
- 历史独立保存最近 31 天，不影响 Codex 计算和历史维护；明确提示记录范围、数据缺口、缓存时效和保存异常。
- 模型与项目、Session 页增加 Claude 额度历史入口；未获得的 Token 和费用不从额度百分比换算。

## Windows 0.8.27 — 2026-09-22

- 概览顶部统一展示各 AI 的额度卡片，Codex 的读取方式、更新时间、重置与估算信息归入自身卡片；当前额度页的补充窗口和数据说明也归入 Codex。
- 概览、模型与项目、Session 明细新增 Codex／Claude 统计标签，跨页面保持选择；日期、搜索、排序、趋势和操作只作用于当前 AI。
- Claude 尚未接入的 Token、请求、Session 与估值统计显示明确说明，不显示虚假的零值；数据源未启用时显示独立提示，切换标签不改变采集开关。
- Codex 的采集、通知、估算设置以及归属确认、修复、清除与重新采样操作明确标注服务商，中英文同步。

## Windows 0.8.26 — 2026-09-22

- 托盘小窗的 Codex 额度估算与详情入口移入 Codex 卡片，放在重置倒计时下方，不再显示在 Claude 卡片外。
- 为估算预留稳定的两行空间，关闭估算时收起该区域；保留原有估算、刷新和数据源开关逻辑。

## Windows 0.8.25 — 2026-09-22

- 移除托盘小窗的“Codex · 今日 Token／今日请求数”卡片及其提示，不保留空白占位，小窗按剩余内容收紧高度。
- 保留 Codex、Claude 额度卡片、估算信息和设置入口；主窗口用量统计与后台采集不变。

## Windows 0.8.24 — 2026-09-22

- 下拉菜单展开面板统一使用不透明纯色背景和清晰边框，移除亚克力毛玻璃的透底与纹理干扰。
- 适配浅色、深色和系统高对比度配色，保留原生选项、选中提示及选择逻辑。

## Windows 0.8.23 — 2026-09-22

- 数据来源设置中，Codex 与 Claude 分别放入独立圆角卡片，以边框、背景和组间留白区分不同服务商。
- 沿用应用的浅色、深色与高对比度卡片样式；保留原有启用开关、路径、手动套餐和保存逻辑，不增加额外折叠层级。

## Windows 0.8.22 — 2026-09-22

- 托盘小窗的 Claude 5 小时与每周额度各增加剩余进度条；缺失或已到重置时间时显示淡色空轨道，不猜测已恢复满额。
- 在数据来源的 Claude 设置中增加手动套餐选择，概览、当前额度和小窗同步显示，并明确标注“手动设置”；默认未设置，不参与额度计算。
- 小窗固定预留进度条和套餐位置，刷新、错误提示或套餐文字变化时保持布局稳定。

## Windows 0.8.21 — 2026-09-22

- Codex 额度卡片的本机登录说明改为“在线查询 · 使用本机登录状态”，避免将账号缓存误解为额度缓存。
- Claude 正常额度快照说明改为“本地缓存 · 由 Claude 更新”；保留较旧、缺失和读取失败等状态提示。
- 同步英文文案，不改变登录、数据采集、刷新和估算逻辑。

## Windows 0.8.20 — 2026-09-22

- 统一 Codex 与 Claude 数据源设置的标题、启用开关和纵向输入项布局，支持单独启用、同时启用或全部停用。
- 停用 Codex 后停止日志监听和扫描、额度查询、账号续验、采样与通知，并隐藏其卡片和统计；设置、索引及历史保留。旧配置默认保持 Codex 启用。
- 重新启用 Codex 时先建立采样边界并读取新快照，重建内存中的连续账号观察。停用期间日志可补扫，但不自动归属到当前账号，也不跨停用区间估算。
- 若边界持久化失败，保持暂停并重试，重启后仍执行该保护；开关保存失败时恢复原值。
- 数据源正在保存时，在修改内存配置前拦截交叉保存或授权，避免带入另一个入口尚未保存的路径。
- Claude 停用继续停止本地缓存读取并隐藏卡片，不退出 Claude、不清理其文件，也不影响另一个数据源。

## Windows 0.8.18 — 2026-09-22

- 概览的额度卡片标题简化为“Codex”，旁边显示套餐，不再显示“在线额度摘要”。
- 托盘小窗的套餐标识移入顶部 Codex 额度卡片标题行，移除底部独立套餐行。
- 保持小窗标题行固定高度，保留缓存状态提示；刷新和切页使用一致的概览标题布局，不改变读取、采样或计价逻辑。

## Windows 0.8.17 — 2026-09-22

- 将 Claude 桌面版设置归入“数据来源”，与 Codex 路径按服务商分区显示，移除独立顶层分组和多余的折叠层级。
- 保留 Claude 开关、目录、缓存来源、保存并重新读取及缓存检测功能，不改变已保存配置、用量数据或额度计算。
- 增加设置分组回归检查，并验证两个保存入口、离开后重新进入与进程重启后的配置保持。

## Windows 0.8.16 — 2026-09-22

- 调整当前额度页面：Codex 名称、账号说明和小号套餐标识收进 Codex 卡片，与 Claude 卡片统一标题结构。
- Codex 多个通用额度窗口放在同一张卡片内；启用 Claude 时两张服务商卡片优先并排，窄窗口自动上下排列，Spark 专属额度仍独立展示。
- 保留剩余百分比、估算金额、详情入口、重置时间与次数，不改变额度读取、计价、账号归属或采样逻辑。

## Windows 0.8.15 — 2026-09-22

- 修复开启 Claude 后点击右上角“保存设置”未保存 Claude 配置、重新进入页面开关恢复关闭的问题；全局与区域内保存统一处理开关、目录及来源。
- 后台刷新保留尚未保存的缓存来源选择；更换目录时清除旧目录的来源选择，未改变 Claude 设置时不重置已有快照。
- 保存前校验目录，写入失败时恢复原 Claude 配置，不将失败显示为已生效；不改变 Codex 用量、账号归属和采样规则。

## Windows 0.8.14 — 2026-09-22

- 安装、覆盖升级及卸载在修改文件前检查实际管理员权限，避免权限不足时逐个弹出回滚文件安全设置错误；不删除系统回滚目录，不关闭回滚保护。
- 应用内 MSI 升级请求 UAC 授权，取消授权后保留原安装并正常重启应用；提权只用于安装器，不用于应用。
- 提供带 MSI 校验的手动安装入口，沿用当前用户已登记的安装目录，并阻止通过另一 Windows 账号提权造成归属变化。
- 保持当前用户安装范围及本地统计数据不变；增加升级、取消授权和安装预检回归。

## Windows 0.8.13 — 2026-09-21

- 新增 Claude 桌面版只读额度快照：可同时查看 Codex 和 Claude 的 5 小时／每周剩余额度与实际缓存重置时间，无需额外登录。
- 设置中提供开关、数据目录和缓存来源选择；缓存过期、并发更新、损坏或来源冲突时明确提示，不伪造刷新时间或重置后的满额。
- Claude 不参与 Codex Token、估值、归属及采样；托盘小窗的 Claude 区域使用固定高度，状态变化不重新缩放。
- 保留屏幕分辨率、DPI 和工作区变化后的托盘小窗适配修复。

## Windows 0.8.12 — 2026-09-21

- 修复分辨率、屏幕缩放及工作区改变后托盘小窗越界、卡片偏移或异常缩小的问题；可见小窗自动重新适配当前屏幕，隐藏小窗在下次打开时校正。
- 统一小窗内容宽度与物理像素换算，约束窄屏下的最小窗口宽度；正常数据刷新仍保持窗口位置和尺寸稳定。
- 不改变 Token 记录、账号归属、计价与额度采样数据。

## Windows 0.8.11 — 2026-09-15

- Session 名称改为只读本机标题索引及状态数据库中的显式名称，独立于账号和额度查询；后台自动刷新，旧记录无需重建用量。
- 修复只更新标题、Token 不变时概览和 Session 明细不刷新的缓存问题。
- 无名称的后台辅助会话显示类别和短 ID，其他缺失名称显示“未获取标题”；不从会话正文生成标题，不改变用量或采样数据。

## Windows 0.8.10 — 2026-09-13

- 托盘小窗改为复用卡片并原位更新数值，为百分比、倒计时、用量和缓存状态保留稳定空间，避免刷新重建引起布局抖动。
- 移除内容刷新后的自动调整窗口高度和贴底定位；打开前在隐藏状态完成测量，避免先显示临时高度再跳动。
- 去掉托盘小窗 ⓘ 说明卡片中的设置详情按钮，保留简短说明、底部设置齿轮及设置页中的完整详情。

## Windows 0.8.9 — 2026-09-13

- 将完整采样说明、估值依据与工作负载构成移至设置的“每周容量估算（实验）”中，内嵌折叠显示，不再打开长详情对话框。
- 额度卡与托盘小窗的 ⓘ 仅保留估值、简短采样状态和设置入口；点击后自动定位并展开详情，小窗不增加滚动条。
- 打开的设置详情随采样与估值状态更新，保留折叠状态及其他未保存设置；无样本时也可查看核验原因。

## Windows 0.8.8 — 2026-09-13

- 按线程、模型和生效时间读取普通／Fast 服务档位及思考强度；区分请求设置、实际响应与未知证据，不将 Ultra 当作独立价格倍率。
- 修复关闭 Fast、明确清空档位或上下文冲突后，后续缺失模式字段可能重新继承旧 Fast 的问题；失效的默认档位须由新的可靠设置重建。
- 旧索引通过独立模式元数据版本安全补齐，升级前备份，保留 Token 计数、已确认归属、采样控制与历史；完成后自动请求重新核验当前估值。
- 增加估值依据与工作负载详情，区分定价覆盖率、模式证据和条件估算，显示有效采样中的模型、模式、思考强度构成。
- 缺失或矛盾的模式证据不伪装为已确认普通档位，未知特殊档位不套其他模型倍率；旧历史估算明确标为未核实参考。
- 保留现有账号、周期与连续性保护；紧凑说明弹层不增加滚动条，完整依据在独立详情中查看。

## Windows 0.8.7 — 2026-09-12

- 中断提示按当前账号、套餐、定价版本及周额度周期过滤，旧周期问题不再显示为当前故障；真正属于当前周期的中断保留原因与时间范围。
- 将采样进度的“待确认”改为“未计入”，区分百分点尚未累计到门槛、等待后续快照、等待日志与等待核验；不足 3 个百分点不再误导为等待两分钟。
- 主额度卡、小窗说明和设置页区分历史参考金额与当前周期采样，历史时间明确不是最后记录时间。
- 提前重置、额度回退或周窗口变化时立即更新展示上下文，不等待后台批次；五小时额度重置不会清空周展示。
- 175 项核心测试通过；同一只读数据副本回放的新旧计算结果完全一致，保留原有采样门槛与归属、连续性校验，不强行合并历史区间。

## Windows 0.8.6 — 2026-09-12

- 手动确认归属后立即请求重算，不再等待五分钟批次；核验期间撤下旧归属结论，将旧估算标为参考。
- 区分归属保存和估算更新：保存后的读取或计算失败不再误报“未修改”，保持待核验并重试；只有最新输入计算成功后才撤下待重算状态。
- 自动刷新开启时，账号身份在三分钟时独立续验，不受额度查询失败退避影响。只读账号，不强刷凭据、不发起登录、不生成额度快照。
- 保留五分钟身份有效期、换号/认证失败失效和计算连续性保护；增加续验、即时重算及剩余中断原因回归测试。

## Windows 0.8.5 — 2026-09-11

- 在“每周容量估算（实验）”内增加修复计算数据、清除计算历史、重新开始采样；操作前确认并备份数据库。
- 修复操作校验日志索引并重算，当前周期的未归属记录仍需用户确认；不强行合并缺证据区间。
- 清除历史保留 Token、归属和原始快照，保存持久化计算起点避免旧估算重新出现；重新采样保留历史参考但隔离旧样本。

- 新会话首次扫描的时间上界改为该文件字节快照捕获时刻，不再使用整轮扫描起点，避免扫描过程中已写入并读到的首条 Token 被误判为未归属。
- 保留连续账号、来源、新文件创建时间与记录时间检查；未来时间、旧导入记录不会因此自动认领。
- 增加扫描期间写入首条记录、换行/完整无换行尾行及重复扫描防重回归测试。

## Windows 0.8.4 — 2026-09-10

- 将扫描账号身份与额度新鲜度分离：成功的账号读取建立仅限当前进程、来源一致且最多 5 分钟有效的归属上下文，额度超时不立即清空它。
- 身份失效、明确换号、认证失败、来源变化立即清除上下文，并取消使用旧身份的进行中扫描；不从历史额度缓存恢复身份。
- 增加独立身份、超时及恢复首轮扫描回归测试；既有未归属记录不会自动批量认领。

## Windows 0.8.3 — 2026-09-10

- 重启检查点持久保留并固定首次恢复终点，允许查询失败后重试及迟到确认。
- 区分暂时身份不可用与明确换号；异常快照先隔离，短断网新增用量通过核验后续接。
- 区间、历史与当前缓存统一事务保存，同值刷新和无变化扫描不再无谓作废计算。
- 增加中断原因说明、冲突输入检查、时钟回拨重试保护及回归测试；151 项核心测试通过。

## Windows 0.8.2 — 2026-09-10

- 重启推定用量通过退出/启动审计、事件匹配及同账号同周期快照核验后，可在计算副本中参与估算；原始归属标签不改。
- 反复查询失败不再永久截断后续快照确认，保留身份、周期和硬边界检查。
- 更新中英文归属说明和诊断文字，增加缺失证据、账号不匹配、确认不足和连续失败回归测试。
- 核心测试 145/145 通过，实际数据副本复算验证无重复计入。

## Windows 0.8.1 — 2026-09-10

- 普通设置保存保留账号上下文和采样进度，仅数据来源或登录模式变化建立隔离边界。
- 查询失败原因不再由账号上下文是否存在决定；断网期间缺少身份时保留核验上下文，恢复成功后仍检查实际账号。
- 保留可恢复的已确认样本，不强行拼接证据不足的待配对区间；说明框和诊断日志增加中断核验提示。
- 增加设置边界、连续失败缺少身份、账号切换、长时间中断及重新核验测试，143 项测试通过。

## Windows 0.8.0 — 2026-09-10

- 采样说明弹窗字号调整为 12 DIP，缩小文字视觉占用。
- 按窗口可用宽度约束说明内容并自动换行，为弹窗内边距预留空间。
- 禁用说明弹窗横向和纵向滚动条，保留点击查看与关闭交互。
- 仅调整说明弹窗呈现，不改变采样算法或历史数据。

## Windows 0.7.10 — 2026-09-10

- 两处采样说明 ⓘ 按钮从 32×32 调整为 22×22 DIP，图标从 16 调整为 12 DIP。
- 去掉按钮常态方块底色及边框，保留点击交互和辅助功能名称。
- 本版仅调整入口样式，不改变采样、账号归属、更新或数据存储逻辑。

## Windows 0.7.9 — 2026-09-10

- 托盘小窗和详细额度页增加可点击的 ⓘ 入口，替代采样与估算文字的悬浮提示。
- 说明框挂载到稳定的窗口容器，刷新时更新说明内容，避免卡片重建中断阅读。
- 支持外部点击或 Esc 关闭；切换页面、隐藏窗口或关闭估算时收起说明，增加中英文辅助功能名称。
- 本版只调整说明入口与交互，不修改采样算法、归属或历史数据。

## Windows 0.7.8 — 2026-09-10

- 启动后额度与索引就绪即执行首次重算，避免等待五分钟期间继续展示旧采样起点；后续保持批量节奏。
- 连接断开使用独立异常类型，不再误记为主动重置；凭精确时间匹配的断线日志兼容恢复旧误分类记录。
- 允许不超过 60 分钟且断线后无用量的空闲中断通过严格核验后续接；保留归属、账号、套餐、周期与完整性隔离。
- 新增旧缓存 7/6 重算为 8/6、跨重启进度保持、空闲与活跃长间隔区别等回归检查，142 项测试通过。

## Windows 0.7.7 — 2026-09-10

- 保留临时查询失败或周期短暂切换前的未成段起点，通过严格连续性检查后续接，不再只恢复已确认样本。
- 用户确认归属后的记录可重新进入对应时间区间；未归属、重启推定或不确定用量不强行续接。
- 限制超时与周期异常的续接时长，要求恢复快照同账号、套餐、定价版本及原周期，且额度与中断前一致；周期异常期间还须无新增用量。
- 扩展续接与拒绝续接回归测试；数据库副本验证确认记录全部纳入且无重复累计。

## Windows 0.7.6 — 2026-09-10

- 区分临时查询失败与主动重置等隔离边界，查询恢复后接回已确认样本，不跨中断配对。
- 通过时间精确匹配的超时日志兼容旧中断记录；保留原始快照并保存恢复依据，避免日志轮转后恢复失效。
- 增加连续超时、硬边界、账号/套餐隔离和旧记录证据匹配回归测试；实际数据离线复算验证通过。

## Windows 0.7.5 — 2026-09-10

- 原额度周期短暂离开后恢复时，接回已确认有效样本，不再清零；不跨异常时段拼接未成段用量。
- 保留账号、套餐、价格目录和停用边界隔离，额度回退时不强行恢复旧样本。
- 增加脱敏额度来源日志：查询通道、身份来源类型、响应分支及周额度数值与重置时间。
- 增加周期往返、异常区间排除及隔离回归测试。

## Windows 0.7.4 — 2026-09-09

- 修复重启后已有有效采样进度暂时归零、退回旧版提示的问题。
- 首次额度查询后恢复同账号、套餐与周期的有效缓存，保留五分钟批量计算节奏。
- 增加部分样本恢复、待确认进度及账号/套餐/重置隔离回归测试。

## Windows 0.7.3 — 2026-09-09

- 周容量采样进度按每个百分点更新，区分已确认与待确认，不再等待累计 3 个百分点才显示进度。
- 待确认进度仅用于展示，保留有效区间及延迟确认门槛，不提前参与估算或归档。
- 更新中英文说明，补充未成段进度与确认后不重复累计的回归检查。

## Windows 0.7.2 — 2026-09-09

- 整理安装包“选择启动入口”页面：统一中文字体、左边距、按钮尺寸和间距，减少多余留白。
- 页面内增加 UsageLoom 图标，精简快捷方式与任务栏说明；不改变安装路径、升级行为或用户数据。
- 增加安装页控件边界、对齐、字体和图标的 MSI 表回归检查，以及不执行安装的预览脚本。

## Windows 0.7.1 — 2026-09-09

- 用 Windows App SDK WinUI 组件替代总包，移除未使用组件依赖，发布文件从 527 项降至 469 项，未压缩体积减少约 23.2%。
- 全新目录发布及打包前依赖/清单检查，避免旧组件残留；增加旧 AI/ML DLL 的更新备份与移除回归。

- 拆分 Dashboard 界面代码，原型迁移到 legacy/codex-plugin，发布说明集中到 docs/releases。
- 打包脚本统一读取项目版本，常规构建输出集中到 artifacts，新增目录指南。
- 保留安装布局、用户数据与原位更新协议；不删除旧缓存或裁剪运行时。

## Windows 0.7.0 — 2026-09-09

- 修复概览切到设置、额度等页面再返回时，日期筛选栏因数据缓存命中而未重新挂载的问题。
- 页面切换使布局缓存失效，保留原日期选择，不改变统计数据或采样历史。
- 导航回归检查验证筛选栏的视觉父级和实际尺寸，不再只检查页面切换成功。

## Windows 0.6.10 — 2026-09-09

- 新版本检查频率可选择从不自动检查、每 1/6/12 小时、每天、每 3 天或每周，立即保存。
- 默认保持每天检查，旧版关闭状态继续保留；手动检查不受自动检查频率限制。

## Windows 0.6.9 — 2026-09-09

- 额度速览右下角增加透明底、镂空齿轮设置入口，包含提示和辅助功能名称；未置顶面板跳转后收起，置顶状态保留。
- 软件更新分组移动到“优先尝试已有登录缓存”下方。
- 托盘“详细统计与设置”拆分为“详细统计”和“设置”，分别直接打开概览与设置；支持首次启动和已有窗口切换。

## Windows 0.6.8 — 2026-09-09

- 更新检查优先使用 Release 附件 `update.json`，公共 API 作为备用；新增打包清单生成脚本与 CI 产物。
- 保存最多 7 天的已知版本信息，检查失败不强制禁用此前已知版本下载；补充中文/英文网络提示与发布页入口。

## Windows 0.6.7 — 2026-09-09

- 设置内检查 GitHub 正式版更新，可每天自动检查；确认后下载、校验 SHA-256、退出、升级并重启。MSI 使用安装事务，ZIP 根据发布清单替换，失败时恢复备份；用户数据目录不参与替换。

- 分页文件缺少旧终点时，仅在同一会话的完整计数序列按顺序全部匹配、且至少有两个不同计数签名时接受历史子集；保留更完整的旧明细，不按单个相同数值删除记录。
- 持久化“缓存历史领先于物理文件”状态；后续追加重新核对序列，无法接续的新增记录继续隔离，防止重复累计。
- 已知影响时间范围的待核对来源仅排除重叠周估算区间；身份或时间范围不明时仍保守暂停。无需清空数据库或全量重建。

## Windows 0.6.6 — 2026-09-09

- 适配 `history_mode: paginated` 迁移：核对会话身份、单调 ordinal 和完整累计/末次计数字段，接续物理索引，保留迁移前计量明细及归属。
- 重定时的最终计数只作为接续证据，不作为新增用量。匹配原终点后，只追加时间严格晚于旧终点的记录；没有可靠终点时继续隔离，不猜测累计差额。
- 已接续文件恢复跨重启增量扫描。首次安装或已丢失旧索引时，不能从分页日志恢复已裁掉的逐次历史；不要清空数据或全量重建来升级。

## Windows 0.6.5 — 2026-09-09

- 增量扫描发现日志缩短、替换、重写或摘要变化时隔离来源，保留原索引与已记录用量；其他正常会话继续采集，不再因一个历史文件变化阻断全部扫描。
- 同属待核对会话的其他文件也不重新推导，避免改变历史基线；重复扫描不重复累计。变化文件尚未核实的新增内容不猜测合并，明确展示待核对数量。
- 存在待核对来源时暂停新增周容量估算，保留历史显示，避免用不完整本地用量生成新结果。此修复不自动执行全量重建或改写原始日志。

## Windows 0.6.4 — 2026-09-08

- 周容量算法 v4：至少 3 个百分点合并为一段，等待至少 2 分钟后的额度快照及日志索引确认，再按累计费用 / 累计百分点计算；迟到日志重新配对，不重复累计。
- 设置页新增参考范围（至少三个已定价区间的最小–最大值，非统计置信区间）及样本成熟度；额度卡片和托盘仍仅显示单个估算值。
- 保留旧算法估算历史作为过渡参考，不混入新样本；账号、套餐、价格及重置边界独立处理。参考范围不代表官方美元额度，也不保证随时间收窄。

## Windows 0.6.3 — 2026-09-08

- 依据明确父会话关系和连续计数前缀排除继承历史；逐项比较累计/单次计数字段及时间顺序，至少三个不同有效累计值，不按跨会话相同数值去重。
- 保留原有 fork 边界判断；无关会话、短匹配、缺失或循环父关系不会被序列去重。不一致计数仅可作为完全相同的历史证据，不作为计费用量或累计基线。
- 解析版本升至 v7；旧索引经验证、备份后重建。增量扫描保留父会话证据，避免重新计入继承历史。

## Windows 0.6.2 — 2026-09-08

- 修复 fork 日志重复父会话标记反复重置累计基线，造成 Token 和 API 等价值严重虚增的问题；父子边界仅在待确认阶段生效一次。
- 解析版本升至 v6，旧索引首次扫描时先验证原始日志，再备份数据库并安全重建；历史估算保留，原始日志不改写。
- 增加 172 次重复父标记、现代子任务边界、跨重启增量和旧索引迁移回归测试。

## Windows 0.6.1 — 2026-09-08

- 修复历史趋势在窄窗口下说明文字与指标按钮共用一行导致的重叠；窄窗口使用标题、指标、说明三行布局，更窄时总数与按钮分行。
- 增加 380/520/900 DIP 宽窄往返的实际布局边界检查，覆盖中英文。

## Windows 0.6.0 — 2026-09-08

- 将周容量重算与明细清理移到后台，串行执行，避免长计算占用界面线程；计算完成时核对用量快照、账号、套餐、设置世代和额度时间，过期结果不覆盖当前界面。退出时取消计算并等待后台任务。
- 显示计算状态、等待原因、上次计算与下次批量检查时间；手动计算不再静默无响应。保留之前有效估算与历史标识。
- 概览新增按日期查看和确认 Token 归属：预览数量，区分未归属、其他账号及无时间记录；事务写入前自动备份，保存独立确认记录以支持重建后恢复；不修改原始日志或覆盖其他账号。
- 新增 Windows GitHub Actions：.NET 回归、自包含构建、MSI 结构与 ZIP 产物检查；不自动发布 Release。
- 包含 0.5.7/0.5.8 的五分钟批量计算和按天保留清理机制。完整说明见 `docs/RELEASE-0.6.0.md`。

## Windows 0.5.8 — 2026-09-08

- 额度快照与配对明细每天自动清理，保留最近 30 天并保护完整配对周期，必要时延长保留；没有可靠周期边界则保守保留。
- 过期区间的估算结果先独立归档，再删除明细；Token 统计、账号归属、原始日志及估算结果保留。过期快照删除后不能追溯重算。
- 设置增加清理说明，运行日志记录删除数量。SQLite 空闲页留待复用，不自动执行全库压缩。

## Windows 0.5.7 — 2026-09-08

- 周容量估算改为有新数据时每五分钟批量计算，打开面板不触发重算；额度快照与日志仍照常采集，等待期间保留上次结果。
- 设置中的周容量估算增加“立即计算”入口与批量计算说明；用户确认补归属及迟到日志在下一批统一重算。

## Windows 0.5.6 — 2026-09-08

- 修复补归属工具将索引字符串写成对象造成的扫描失败：兼容识别已知的用户确认用量记录，序列化时恢复字符串格式，保留归属和精确时间；其他损坏仍报错。
- 历史估算回显不再依赖扫描完成，只在核验当前账号与套餐后展示；不因此创建新采样。

## Windows 0.5.5 — 2026-09-08

- 周容量 v3 改为持久化查询快照并按 (起点, 终点] 配对用量事件；额度平台期保留起点，原始小数精度用于计算。
- 用户确认补归属和迟到日志重算替换区间，不再永久排除或重复累计；自动推定归属仍不采样。区间保留事件 ID、端点、费用与排除原因。
- 账号、套餐、重置、关闭采样和身份核验失败形成边界；不跨边界配对。旧版缺失的历史快照不能补造，旧结果只可作为历史参考，不混入 v3 样本。
- 查询日志注明本次响应是否包含小数；整数响应不伪造精度。计费延迟及其他设备用量仍可能影响估算，说明已更新。

## Windows 0.5.4 — 2026-09-08

- 额度重置或回升后保留同账号、套餐和计价口径的上次有效估算作为带时间标记的参考；当前周期独立采样，达到有效门槛后自动替换。历史展示不进入新周期样本，支持跨重启恢复。

## Windows 0.5.3 — 2026-09-08

- 用户确认补归属的记录保留独立标记，在概览注明，并排除在周容量估算之外；重新扫描保留归属。

## Windows 0.5.2 — 2026-09-08

- 正常退出时保存最近有效账号与日志索引边界；下次启动核验相同账号后，仅对边界内新增且未归属的记录进行跨重启推定归属。
- 推定记录在概览注明，保留本地审计标记，排除在周容量采样之外；旧日志、不同账号、时间不明或登录失败不自动归属。
- 首次升级没有旧退出边界，不追溯修改已有未归属历史。异常退出或退出时没有有效连续扫描状态时，仍采用保守归属。

## Windows 0.5.1 — 2026-09-08

- 展开面板改为一次完成内容显隐，移除默认模板先位移、再延迟缩减高度的收起动画；保留原生标题、箭头、键盘和无障碍行为。

## Windows 0.5.0 — 2026-09-08

- 个性化恢复并改为默认收起的展开分组，提供外观和语言选择。
- 外观选择立即应用并自动保存，无需点击保存设置，也不等待额度后端重连；主窗口与托盘小窗同步切换。
- 新增简体中文和 English，语言自动保存，完全退出并重新启动后生效。主页面、图表、设置、托盘菜单、通知和估算说明使用双语资源；用户会话名、项目名、模型标识及原始诊断信息不强行翻译。
- 语言与外观设置不重置额度采样或更改账号归属；MSI 和 ZIP 继续共用现有本地数据目录。

## Windows 0.4.9 — 2026-09-08

- 数据来源与更新节奏改为默认收起的可展开面板，与其他分组一致；仅一项的个性化保留直接显示，设置和保存逻辑不变。

## Windows 0.4.8 — 2026-09-08

- 设置中的每周容量估算改为默认收起的可展开面板，与其他设置分组一致；展开查看开关、结果、历史及计算说明，不影响后台采样。

## Windows 0.4.7 — 2026-09-08

- 周美元估算并入通用周额度卡片，显示在百分比右侧，字号更小。
- 周容量功能开关、历史、采样状态和计算说明移入设置；开关立即保存，关闭停止采样、隐藏结果但保留历史。
- 重新开启或保存设置后恢复可用样本并重新建立起点，不配对关闭期间消耗。

## Windows 0.4.6 — 2026-09-08

- 连续同账号扫描期间新建的会话文件，其观察区间内记录可在首次扫描时归属；保留启动前历史、导入旧记录、账号切换和重建保护，不批量补标旧数据。

## Windows 0.4.5 — 2026-09-08

- 置顶按钮改为标题栏透明样式，与窗口控制按钮顶部对齐；悬停浅底，置顶用紫色图钉标识，不再常驻方块背景。

## Windows 0.4.4 — 2026-09-08

- 托盘小窗为周容量固定预留两行，采样中与估算完成不再因行数变化调整窗口高度和位置；超长文本可悬停查看全文。

## Windows 0.4.3 — 2026-09-08

- 新增本地用量后 5 分钟内，后台采用活跃查询周期，无需打开面板；空闲后恢复后台兜底，保留自动刷新开关和失败退避。
- 持续日志写入不再无限推迟增量扫描；增加后台采样活动日志。

## Windows 0.4.2 — 2026-09-08

- 自定义日期统一显示年-月-日，避免区域格式导致月日混淆。
- 自定义同一天按所选日期显示 24 小时趋势，不再只绘制一个日汇总点；多天保持按日聚合。

## Windows 0.4.1 — 2026-09-08

- 估算历史独立持久化、去重并分页查看，保留套餐、周周期、算法与定价版本。
- 空采样、重启、换套餐、重置采样和重建日志不覆盖历史；现存有效旧缓存首次读取时归档。
- 小窗置顶改为标题栏图钉按钮，放在版本号与最小化按钮之间，避免占据正文操作区。

## Windows 0.4.0 — 2026-09-07

- 首个 Windows 桌面 Release，提供自包含 MSI 与 ZIP，统一用户数据目录。
- 本地用量、小时/日期趋势、模型项目与 Session 明细、账号观察归属。
- 官方后端额度查询、Spark 独立窗口、套餐标识与可选通知。
- 添加应用图标、缩小套餐标识、K/M/B 数字与更窄的可置顶托盘面板。
- 修正周容量采样的账号范围；废弃旧口径样本。该功能仍为实验估算，不代表官方美元额度。
- 安装包结构和自动化测试不替代干净系统与多屏人工验收；发行文件尚未签名。

本文件记录 UsageLoom 各版本中值得关注的变化。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## Windows 0.8.4 — 2026-09-10

- 0.3.21：主 Codex 额度单独展示，其他额度折叠并优先使用服务端名称；主周容量估算不再混入其他额度组。倒计时按天、小时、分钟显示，微小消耗不再四舍五入成 100%。
- 0.3.20：隔离会话标题与额度 RPC 连接，标题响应损坏不再中断额度查询；断开的连接立即报错，并修正未收到账号响应时误报已登录的提示。
- 0.3.19：自动发现桌面 App 版本目录中的额度后端，手动路径失效后重新查找；找不到程序时明确提示后端定位失败。
- 历史趋势第二项由去重 Session 数改为带 Token 的完成请求数，并明确本地会话日志无法还原失败、取消和零 Token 的网络尝试。
- 修复估算价值遗漏可验证长上下文倍率的问题：解析索引升级到 v5，保存可靠的单请求输入规模；Astra 超过 272K 输入时按官方规则计入输入/缓存 ×2、输出 ×1.5。继续采用 OpenAI 当前公开价格，不为匹配采用旧价格目录的第三方工具而改价。
- 修复 Codex 桌面端同一任务跨多个 rollout 文件时独立累计计数被旧高水位误判为历史回放、导致近期 Token 大量漏记的问题；解析索引升级到 v4，安装新版后会先备份旧库再从原始日志安全重建。
- 修复旧版派生事件身份与累计总量变化后，增量索引永久拒绝后续数据的问题；索引格式升级时先验证原始日志、备份 SQLite，再事务重建。
- 重建现在区分原始 JSONL 结构损坏与可恢复的数据质量提示：结构损坏、前缀变化或意外空结果继续保留旧库；超大无关行、活动半行、fork/累计重置等可恢复提示不再让迁移永远无法完成。
- 主窗口限制为至少 620 DIP 宽，额度悬浮窗限制为至少 380 DIP 宽，并按当前窗口 DPI 换算，避免缩成无法阅读的竖缝。
- 版本号移至始终可见的窗口标题栏徽标；紧凑侧栏不再渲染会被 `NavigationView` 模板裁切的页脚，展开侧栏时仍显示完整说明。
- 登录状态下分开展示所选范围的本机全部历史、当前账号观察归属和未归属/其他账号用量；只对前后扫描账户指纹连续一致的新增量做前向归属，不倒推旧日志。
- 修复收起侧栏把 `0.3.11` 等版本号换行裁剪成类似 `0.3.1` 的问题；紧凑模式完整居中显示版本，展开后恢复本地优先说明。
- 修复受影响 Session 的增量高水位重新归属被误判为历史删除、导致当天新增记录无法入库的问题；现在只事务替换已完整重放的 Session，并继续拒绝累计总量回退。
- 历史趋势改为按卡片实际宽度原生重排，避免窄窗口下悬浮提示、文字与图形被非等比压缩；横轴标签密度随可用宽度调整。
- 默认外观改为跟随 Windows 系统主题，并迁移旧版隐式深色默认值；修复历史趋势未选中指标按钮在深色模式下文字不可见。
- 重新设计 WinUI 历史趋势：新增日、近 7 天、本周、本月和自定义范围，支持 Token、请求数与估算价值切换，并提供缺失日期补齐、长区间压缩、悬停明细和点击下钻。
- 明确当前为 JavaScript 技术原型，独立 Windows 客户端尚在开发准备阶段。
- 将本地规划和开发流水与公开文档、CI 校验分离，公开源码副本不依赖内部材料。

## 0.1.0 原型 — 2026-09-04

- 建立 UsageLoom 首个技术原型，提供本地优先的 Codex Provider Adapter；该记录不代表已公开发布 Windows 产品。
- 新增本地 JSONL Token 归因、实时额度窗口、重置时间和 API 等价费用估算。
- 新增只读 MCP 工具和自包含的 MCP Apps 仪表盘。
- 新增隐私、架构、贡献、安全、发布和第三方声明文档，为公开仓库做准备。
- 新增有边界的诊断脱敏机制，并把任务标题索引读取改为用户主动选择。
- 说明 UsageLoom 受到 `zJay26/codex-usage` 和 `Nirlep5252/CodexBarWindows` 启发，并通过独立实现把两者互补的产品理念结合起来。
