# UsageLoom 0.4.0

首个 Windows 桌面正式发布。Windows 11 x64；MSI 安装版和自包含 ZIP 免安装版均附带运行库。

## 功能

- 本地 Token、完成请求、模型/项目/Session 分析与小时/日期趋势。
- 通过官方后端读取已有缓存账号额度；通用额度和 Spark 独立展示。
- 套餐标识缩小；补齐应用图标；K/M/B 数字和 380 DIP 托盘速览，增加置顶。
- 可选通知、深浅主题、统一用户数据目录和采样持久化。
- 修复周容量采样混入其他账号/未归属历史；旧口径样本重新采集。累计至少 5 个百分点、2 段配对后显示估算。

## 使用与升级

退出旧版后安装 MSI，或完整解压 ZIP 运行 UsageLoom.App.exe。两者共用 %LOCALAPPDATA%\UsageLoom\user-data\，无需迁移数据。新版采样规则改变会重新采样，但不会删除 Token 历史。

## 限制

- 周容量为实验性的 API 等价外推，不是官方周美元额度；缺价、其他设备/共享用量及采样延迟可能造成明显偏差。
- 账号观察归属不等同于官方跨设备总使用量。
- 未签名；请从本仓库下载并核对 SHA256SUMS.txt。
- 自动化页面导航与 MSI 结构验证不替代干净系统安装或多屏 DPI/置顶人工验收；本版这些人工项目尚未完成。
- 索引仍按受影响 Session 重放，未实现完整数据库级增量聚合。

README 包含实际运行截图与使用说明。
