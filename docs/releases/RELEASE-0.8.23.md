# UsageLoom 0.8.23

## 更新内容

- “设置 → 数据来源”中的 Codex 与 Claude 分别使用独立圆角卡片，通过边框、背景和间距清晰分组。
- 卡片沿用浅色、深色及高对比度主题样式，不增加额外折叠层级。
- 保留启用开关、路径配置、手动套餐及保存行为，不改变数据读取、账号归属或额度计算。

## 升级

可通过软件内检查更新升级。手动安装请将同版本 MSI 和 `UsageLoom-install.cmd` 放在同一目录，从托盘退出 UsageLoom 后运行安装入口并确认 UAC。无需卸载或清理数据。
